server:
  port: 10014
  servlet:
    encoding:
      enabled: true
      force: true
      charset: UTF-8
spring:
  application:
    name: spring-ai-alibaba-dashscope-multi-model-example-application
  ai:
    dashscope:
        api-key: ${AI_DASHSCOPE_API_KEY}
        chat:
          options:
            model: qwen3.6-plus
